/*
Variables defined in the test suite:
- oldID
- currentAge

You should NOT try to redefine any of the above variables.
*/

// Write your code below this line!
let multiply = "10000000" + oldID;
let newID = Number.parseInt(multiply, 10);

let randomNumber = (Math.random() * 20) + 1;
let random = Math.random();

let ageIsValid = Number.isInteger(currentAge);

let randomInteger = Math.round(randomNumber);

let addNumber = "10000000" + randomInteger

let randomUserID = Number.parseInt(addNumber, 10);
